#include <iostream>
using namespace std;

int main() {
    cin.tie(NULL);
    cout.tie(NULL);
    ios_base::sync_with_stdio(false);
    
    int asc, des, mix, scale[100];
    for (int i=1; i<=8; i++)
    {
        cin >> scale[i];
    }

}